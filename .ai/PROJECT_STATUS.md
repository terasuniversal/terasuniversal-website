# PROJECT_STATUS

Last updated: 2026-08-28
Repository: `D:\Projects\terasuniversal-website`
Branch: `chore/ai-shared-project-state`

## Current state

- TERAS Universal Next.js 15 application containing the public website and the Supabase-backed admin/CRM.
- Root `CLAUDE.md` already contains the detailed engineering, database, security, and review rules.
- Shared agent state is maintained in this folder. `.ai/CURRENT_TASK.md` is an existing generated router record for an older attendance-print task and is preserved as historical task state.
- Root `AGENTS.md` now defines the cross-agent workflow and approval boundary.

## Agent ownership

- Codex: primary implementation for the active task and independent technical review where needed.
- Claude Code: architecture, security, database, auth/RLS, certificate, and cross-module review or implementation when assigned.
- DeepSeek: low-risk, routine, narrowly scoped implementation only.
- Human: explicit approval for commit, push, merge, deploy, and production migration application.

## UI source of truth

- Admin primitives: `components/admin/ui/index.tsx`
- Admin styling: `app/admin/admin.css` and `ta-*` class conventions
- Public styling: `app/globals.css`
- Keep public and admin styling separate; record any deliberate Design System change in `.ai/DECISIONS.md`.

## Worktree note

The checkout contains substantial pre-existing modified and untracked application, CRM, documentation, asset, and Supabase files. Those changes predate this state setup and must not be reset, cleaned, or folded into unrelated work.

## This setup task

Completed the shared-state baseline by adding root `AGENTS.md`; root `CLAUDE.md` already contains the corresponding shared-state and approval rules; and adding this status file plus `.ai/DECISIONS.md` and `.ai/ROADMAP.md`. Validation results are recorded in the final task response and should be refreshed after any subsequent implementation task.

## Validation for shared-state setup

- `npm run lint`: PASS — syntax check passed for 247 JavaScript files.
- `npm run build`: PASS — Next.js production build completed and generated 80 static pages.
- `npm run typecheck`: BLOCKED by the local runtime before TypeScript starts: `spawn EINVAL` from `scripts/typecheck.mjs` under Node `v24.18.0`; `package.json` requires Node `22.x`.
- `git diff --check`: existing unrelated binary/PDF worktree changes produce trailing-whitespace warnings; no source file was changed by this setup task except `CLAUDE.md` and the new Markdown state files.

## Open risks / follow-up

- The existing generated `.ai/CURRENT_TASK.md` is blocked and stale relative to this setup task; do not hand-edit it because its header says it is regenerated by the router.
- Production schema claims must be re-verified against the connected Supabase project before consequential database work.
- The branch is ahead of and behind its remote; reconcile only with explicit scope and approval.

## CRM Marketing continuation — 2026-08-28

- Audited the existing Marketing Campaigns and Marketing Contacts routes, actions, library helpers, and related migrations.
- Verified read-only against Supabase production (`iagzkrzeuawaxvacqprk`) and staging (`pzgtyskhyhuxhzvyzzhe`): marketing tables exist; production has 0 campaigns/contacts/events, staging has 0 campaigns, 1 contact, and 1 event.
- Fixed `app/admin/(protected)/marketing/contacts/actions.ts`: duplicate detection now accepts a typed Supabase client, uses a narrow `MarketingContactDuplicate` shape, and checks/logs errors from both email and phone duplicate queries instead of dropping them.
- Validation: `npm run lint` PASS; `npx tsc --noEmit` PASS; `npm run build` PASS.
- No migration, production data mutation, commit, push, merge, or deployment was performed.

## CRM Marketing continuation — 2026-08-28 (follow-up)

- Removed remaining Marketing Contacts/Campaigns UI `any` casts in option loaders and detail value rendering.
- Added error-aware handling to staff, course, and campaign option queries; null staff names now render as `Unnamed staff`.
- Replaced unvalidated URL filter casts with allow-listed status/channel/source values from the existing validation constants.
- Validation: `npm run lint` PASS; `npx tsc --noEmit` PASS; `npm run build` PASS.
- No database mutation or production release action was performed.

## CRM Marketing continuation — 2026-08-28 (dashboard)

- Replaced the Marketing landing redirect with a live read-only dashboard at `/admin/marketing`.
- Added KPI cards for total/active campaigns, total contacts, sales-ready contacts, and due follow-ups, plus direct links to Campaigns and Contacts.
- Added the Marketing Dashboard entry to the existing admin navigation using the existing `campaigns` icon and `marketing` RBAC module key.
- Dashboard query failures render an explicit error state; no counts are fabricated.
- Validation: `npm run lint` PASS; `npx tsc --noEmit` PASS; `npm run build` PASS.
- No database mutation, migration, commit, push, merge, or deployment was performed.

## CRM Marketing continuation — 2026-08-28 (newsletter capture)

- Updated `app/api/newsletter/route.js` so successful Resend subscriptions are also captured as `marketing_contacts` with `source = newsletter` and `consent_status = opted_in`.
- Existing contacts are updated rather than duplicated; new contacts receive a timeline event. CRM persistence is best-effort and never changes a successful Resend response into a failure.
- Service-role access is used only in this server-only route and is skipped with an explicit server log when `SUPABASE_SERVICE_ROLE_KEY` is not configured.
- Validation: `npm run lint` PASS; `npx tsc --noEmit` PASS; `npm run build` PASS.
- No migration or direct production write was executed during this task.

## CRM Marketing continuation — 2026-08-28 (release configuration)

- Documented `RESEND_AUDIENCE_ID` and the server-only `SUPABASE_SERVICE_ROLE_KEY` requirement in `.env.admin.example`, `.env.cms.example`, and `docs/release/go-live-checklist.md`.
- Confirmed the local environment does not expose a service-role key; no secret values were added or printed.
- Staging end-to-end newsletter delivery remains pending because Resend Audience credentials are not configured in this session.

## Claude Code review — 2026-08-28 (Codex's Marketing→Sales promotion + is_test reconciliation work)

**Scope reviewed**: the newest uncommitted work on `feat/public-training-ux` as of this session — Marketing CRM Phase 1A–1D (`marketing_campaigns`, `marketing_contacts`, `marketing_contact_events`, dashboard, newsletter capture) plus the two migrations layered on top today: `20260828100000_widen_sales_lead_source_for_marketing_contacts.sql`, `20260828110000_promote_marketing_contact_to_sales.sql` (new `promote_marketing_contact_to_sales` RPC + `v_sales_lead_inbox` 3-way source fix), and `20260828120000_reconcile_sales_is_test_classification.sql`. Also spot-checked the Sales-side consumers (`lib/sales/crm.ts`, `sales/leads/[id]/page.tsx`, `sales/leads/page.tsx`, `sales/opportunities/[id]/page.tsx`, `sales/reports/page.tsx`) and `PromoteToSalesPanel.tsx` / `marketing/contacts/actions.ts`.

**Verdict: PASS.** No blocking defects found. Findings below are follow-ups, not blockers.

### Validation run this session
- `npx tsc --noEmit` — **PASS**, no errors (run directly; the `npm run typecheck` wrapper script still fails under the local Node 24 runtime vs. the `22.x` `engines` pin — pre-existing environment issue, unrelated to this work).
- `npm run lint` — **PASS** ("Syntax check passed for 247 JavaScript files" — note this script only syntax-checks the public-site `.js` files, it does not lint the admin TypeScript that was actually changed).
- `npm run build` — **PASS** on a clean re-run (all `/admin/marketing/*` and `/admin/sales/*` routes compiled and listed; a first invocation printed a stray "Could not find a production build" line after a successful compile, not reproducible on retry — worth a glance if it recurs, not treated as a real failure here).

### What checked out
- New Server Actions (`promoteMarketingContactToSales`, contacts/campaigns CRUD) all guard-first (`requireRole("editor")` / `requireModuleAccess(...)`), validate via Zod, check `{ error }`, and revalidate — matches CLAUDE.md §9.
- RLS on `marketing_contacts` / `marketing_contact_events` uses `app.has_min_role('editor')`, not a parallel authorization table — matches §6/§8.
- The new `promote_marketing_contact_to_sales` RPC independently re-enforces the editor+ role gate inside `SECURITY DEFINER` (doesn't trust the calling Server Action alone), uses `for update` row locking, and has two independent idempotency backstops (app-level check + the live `UNIQUE(lead_source, source_id)` constraint) — sound design, well-commented on the *why*.
- `v_sales_lead_inbox`'s CASE logic was correctly widened from binary to 3-way for the new `marketing_contact` source; every downstream consumer of `SalesLeadSourceKind` / `SOURCE_LABELS` (`lib/sales/crm.ts`, leads list/detail, opportunity detail, reports) was updated consistently — no stale two-way switch left behind anywhere I checked.
- `lib/supabase/database.types.ts` was extended for `marketing_campaigns` / `marketing_contacts` / `marketing_contact_events` (§4/§11 rule 8 followed); no new `as any` casts introduced by this work (grepped the touched marketing/sales files — the one hit in `sales/opportunities/[id]/page.tsx` is pre-existing, not part of this diff).
- `app/api/newsletter/route.js`'s new `createSupabaseServiceClient()` call site is server-only, gated on `SUPABASE_SERVICE_ROLE_KEY` presence, checks `{ error }` on every call, and is wrapped so a CRM-capture failure never turns a successful Resend subscription into a user-facing error — reasonable use of an otherwise-zero-call-site client for a public unauthenticated write RLS can't grant.
- Migrations are idempotent/guarded, single-purpose, no destructive DDL, and none have been applied to any environment by this work (still sitting as files in `supabase/migrations/`) — consistent with "no migration, commit, push, merge, or deploy" being performed.

### Follow-ups (non-blocking)
1. **`20260828120000_reconcile_sales_is_test_classification.sql`'s own header claims** the `is_test` feature is "already LIVE in production... applied outside the tracked migration history" and that every statement in the file is "written to be a no-op there." **I could not independently verify this** — no Supabase MCP tool was available in this session, so I have not confirmed live schema state myself. Per CLAUDE.md §11 rule 1 / AGENTS.md, re-verify this against the connected Supabase project (staging `pzgtyskhyhuxhzvyzzhe` and production `iagzkrzeuawaxvacqprk`) before this migration is ever applied anywhere — an undocumented production schema change applied outside migration history is itself worth a second look, independent of whether this file correctly reconciles it.
2. `work/public-website-phase2b3-batch2/` and `work/public-website-phase2b3-batch3/` are new `git worktree` checkouts added under `work/` today (~1.1 GB combined, confirmed via `git worktree list` — legitimate worktrees, not stray copies). CLAUDE.md §2a already flags `work/` as dead weight not to be grown further without a cleanup plan; these are unrelated to the marketing/sales work reviewed here but sitting in the same working tree. Recommend removing with `git worktree remove` (not `rm -rf`) once their task is done, rather than leaving them to accumulate.
3. `public/downloads/TERAS-UNIVERSAL-Company-Profile.pdf` shows a large working-tree diff (~3.7 MB → ~104 MB) — this predates today's session (already listed as a pre-existing change in the stale `.ai/CURRENT_TASK.md` router record from 2026-08-13) and is unrelated to the marketing/sales work, but is still sitting uncommitted and is worth someone confirming is intentional before it's ever committed.

No commit, push, merge, migration apply, or deployment was performed as part of this review.

## CRM Marketing continuation — 2026-08-28 (live RLS/grants audit)

- Read-only Supabase audit completed for production (`iagzkrzeuawaxvacqprk`) and staging (`pzgtyskhyhuxhzvyzzhe`).
- `marketing_campaigns`, `marketing_contacts`, and `marketing_contact_events` have RLS enabled in both environments.
- Policies match the intended boundary: `authenticated` users with minimum `editor` role can select; campaigns/contacts can insert and update; contact events can insert and select only. No DELETE policy or anonymous grant was found.
- `authenticated` table grants match the migrations; no database mutation was performed.
- Supabase security/performance advisors were also run read-only. Returned warnings are pre-existing findings on unrelated tables/functions/indexes; no Marketing-specific warning was identified in the returned advisor output. These broader findings remain for a separate audit.

## CRM Marketing continuation — 2026-08-28 (browser smoke check)

- Local browser smoke check requested `/admin/marketing` and confirmed the protected route redirects unauthenticated users to `/admin/login?next=/admin/marketing`.
- No credentials were entered and no CRM form was submitted; authenticated Campaigns/Contacts UI checks remain pending an approved signed-in test session.

## CRM Marketing continuation — 2026-08-28 (router state check)

- The official router status command confirmed `.ai/CURRENT_TASK.md` still describes a stale blocked Attendance task (`Scope: FAIL`), not the current Marketing work.
- An official dry-run attempt to reconcile the work item was made without running an implementer or changing code/database state, but it could not persist because `.ai/task-state.json` returned a filesystem permission-denied error.
- `CURRENT_TASK.md` was not hand-edited. Reconciliation remains blocked on repository file permissions or the approved task-runner environment.

## CRM Marketing continuation — 2026-08-28 (follow-up filter)

- Added a focused `followup=due` Contacts filter and linked the Marketing Dashboard's Follow-ups Due KPI to it.
- The filter uses the same due-date and terminal-status rules as the dashboard count, and preserves the filter through pagination/reset behavior.
- Validation: `npm run lint` PASS; `npx tsc --noEmit` PASS; `npm run build` PASS; scoped `git diff --check` PASS.
- Router reconciliation remains blocked separately by write permission on `.ai/task-state.json`; no database or production action was performed.

## Claude Code fix — 2026-08-28 (is_test migration cross-environment ordering bug)

**Trigger**: the human ran the live-verification I could not run in the previous review (no Supabase MCP tool was available to me that session) and confirmed via Supabase MCP:
- Production ("TERAS Certificate Verification", `iagzkrzeuawaxvacqprk`): `is_test` (boolean, not null, default false) already exists on `sales_lead_metadata`, `sales_opportunities`, `sales_quotations`, and is already exposed in `public.v_sales_lead_inbox` — with **no corresponding tracked migration**. Confirms the drift claim in the reconcile migration's header was accurate. Last tracked migration recorded on production: `20260827170937_widen_sales_lead_source_for_marketing_contacts`. Neither the reconcile migration nor the promote/view migration has been applied there yet.
- Staging ("TERAS Database Baseline Staging", `pzgtyskhyhuxhzvyzzhe`): **no `is_test` column at all**, on any table.

**Bug found and fixed**: the two migrations as originally filed (`20260828110000_promote_marketing_contact_to_sales.sql` replacing `v_sales_lead_inbox`, `20260828120000_reconcile_sales_is_test_classification.sql` adding the `is_test` columns) were **not safe to apply, in either order, to both environments**:
- Applied in original filename order (promote/view migration first, since `110000 < 120000`): on **production**, `create or replace view` would attempt to reproduce a view whose live column list already ends in `..., subject, is_test`, but the migration's replacement `select` list stopped at `subject` — Postgres's `create or replace view` cannot drop or reorder an existing view column, so this would have failed outright on production with `cannot drop columns from view`.
- Even setting that aside, the reconcile migration's own header had asserted staging would already have Phase 1B-D's 3-way source `CASE` applied by the time it ran and left the view "explicitly out of scope" — an assumption that today's live check disproved (neither migration is applied anywhere yet).

**Fix applied** (files only — nothing applied to any database):
1. Swapped which logical migration occupies which timestamp slot: the `is_test` column/index/trigger/`mark_lead_test` migration is now `20260828110000_reconcile_sales_is_test_classification.sql` (runs first), and the promote-RPC/view migration is now `20260828120000_promote_marketing_contact_to_sales.sql` (runs second). Neither file had been applied anywhere, so renaming carried no migration-history risk.
2. `20260828120000_promote_marketing_contact_to_sales.sql`'s `v_sales_lead_inbox` replacement now selects `m.is_test` as the view's trailing column, so the reconciled view matches what's already live on production (verified append-only-column reasoning documented inline) and gains the column fresh — legally, since Postgres only restricts *removing/reordering* existing view columns — on staging.
3. Both files' header comments were rewritten to state the actual verified live state (per the human's fresh MCP check above), explain why the ordering matters, and flag the one remaining unverified assumption (see below). No SQL logic outside the view's column list and the two header comments was changed — column adds, indexes, trigger functions, triggers, and `mark_lead_test` in the reconcile migration are byte-identical to before; the promotion RPC body in the promote migration is byte-identical to before.

**Still not independently verified — flag before applying**: I inferred that `is_test` is the *trailing* column in production's live `v_sales_lead_inbox` from a real Postgres constraint (`create or replace view` can only append new columns at the end, so any prior ad hoc addition could only have landed there) rather than from a direct query. Before this migration is ever applied, run one more live check — `information_schema.columns` ordinal positions for `v_sales_lead_inbox` on production (or `\d+ public.v_sales_lead_inbox`) — to confirm `is_test` is actually last. If it is not, `create or replace view` cannot be used to fix it; the statement would need to become `drop view` + `create view` (re-issuing the existing grants) instead.

**Not done, deliberately out of scope for this fix**: `SalesLeadInboxRow` in `lib/sales/crm.ts` does not yet include `is_test`, and no Sales UI reads/displays it (no test/demo badge, no filter). The view and `mark_lead_test` RPC now expose/manage the data; wiring it into the Sales UI is separate feature work, not part of making these two migrations apply safely.

No migration was applied, no push, no merge, no deploy. Validation not re-run in this pass (no application code was touched, only `.sql` migration files and this status file).

## Claude Code correction — 2026-08-28 (is_test column ORDER: my inference was wrong, fixed with drop+create)

**Trigger**: the human ran the exact live check my previous entry asked for — a direct ordinal-position query against production's `information_schema.columns` for `public.v_sales_lead_inbox` via Supabase MCP (ref `iagzkrzeuawaxvacqprk`) — and it disproved my inference.

**What I got wrong**: I had reasoned that because `create or replace view` can only *append* new trailing columns (never reorder or insert among existing ones), `is_test` must be the *last* column in production's live view, since that's the only way an ad hoc addition could have landed there. That reasoning about the Postgres constraint was correct; the conclusion from it was not, because I was reasoning about how a *fresh addition via `create or replace view`* would have to land — but nothing establishes that whatever untracked process modified production's view actually went through that exact path, and it evidently didn't.

**Confirmed live column order for `public.v_sales_lead_inbox` on production**:
```
1 lead_metadata_id   6 follow_up_at   11 created_at    16 phone
2 lead_source        7 priority       12 updated_at    17 subject
3 source_id          8 lost_reason    13 contact_name
4 status             9 won_at         14 company
5 assigned_to       10 is_test        15 email
```
`is_test` sits at position 10, between `won_at` and `created_at` — not trailing. This makes the fix from the previous entry (appending `m.is_test` after `subject` in a `create or replace view`) **also wrong** and would have failed on production with `cannot drop columns from view` (because reproducing production's real order requires `is_test` mid-list, which a plain append can't do) while simultaneously being unsafe on staging (inserting `is_test` in the middle of staging's current — is_test-less — 16-column view would count as reordering staging's own existing columns, equally forbidden).

**Fix applied to `supabase/migrations/20260828120000_promote_marketing_contact_to_sales.sql`** (file only — nothing applied to any database):
1. Replaced `create or replace view public.v_sales_lead_inbox ...` with `drop view if exists public.v_sales_lead_inbox;` followed by `create view public.v_sales_lead_inbox ...`. Dropping first removes the "must reproduce existing column order" constraint entirely, so one statement can define the single canonical order and apply cleanly regardless of an environment's starting shape (production's drifted 17-column view, staging's original 16-column view, or no view at all on a fresh environment).
2. Moved `m.is_test` in the `select` list to sit between `m.won_at` and `m.created_at`, matching production's confirmed live order exactly.
3. Checked for hard dependencies before relying on a plain (non-`CASCADE`) `drop view`: grepped every tracked migration referencing `v_sales_lead_inbox` (`20260814120000` — the original `create`; `20260814150000`, `20260814165048`, `20260814270000` — all `app.convert_lead_to_opportunity`-family functions). None of them build a view/materialized view on top of it; they all reference it as a plain `select ... into v_lead from public.v_sales_lead_inbox` inside a PL/pgSQL body, which is a runtime name lookup, not a catalog-level (`pg_depend`) dependency — it cannot block `drop view` and keeps working unchanged afterward since it uses `select *` and every column name is preserved. Documented in the file that this doesn't rule out an *undocumented* dependency from the same untracked drift, and that a `pg_depend` check against production is a reasonable final step before applying if extra certainty is wanted.
4. Documented that because `drop view` + `create view` (unlike `create or replace view`) does **not** preserve existing grants, the `revoke`/`grant` statements already present after the view definition are no longer optional belt-and-suspenders redundancy — they are now the only thing restoring the view's access grants, and must stay in the file.
5. Rewrote both the "is_test" comment block and the top-of-file "Ordering / dependency" note to state the now-confirmed real column order instead of the earlier inference, and to explicitly record that the earlier trailing-column assumption was checked and found wrong (not silently replaced).

No other SQL changed: the RPC body (`promote_marketing_contact_to_sales`), the CASE-widening logic for `marketing_contact`, and `20260828110000_reconcile_sales_is_test_classification.sql` (columns/indexes/triggers/`mark_lead_test`) are unchanged from the previous entry.

**Still open before this can be applied anywhere**: the file itself now recommends one more live check — a `pg_depend` query against production for objects depending on `v_sales_lead_inbox` — as a final safety margin given production carries undocumented drift beyond what's already been found. This is a suggestion for extra confidence, not a known blocker.

No migration was applied, no push, no merge, no deploy. Validation not re-run in this pass (only `.sql` migration files and this status file were touched).

## Claude Code fix — 2026-08-28 (v_sales_lead_inbox grants after drop+create were incomplete)

**Trigger**: the human directly queried production's actual grants on `public.v_sales_lead_inbox` (ref `iagzkrzeuawaxvacqprk`) and confirmed:
- `anon` — no privileges at all
- `authenticated` — SELECT only
- `service_role` — SELECT, INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER
- `postgres` — SELECT, INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER

**Gap found**: the previous entry's fix (switching to `drop view if exists` + `create view`) correctly flagged that DROP VIEW wipes existing grants and that the revoke/grant block afterward is no longer optional — but the block that was actually in the file only handled `anon` (revoke all) and `authenticated` (grant select, revoke the rest). It had no statement at all for `service_role` or `postgres`, so after a `drop view` + `create view`, those two roles' full access would not have come back unless Supabase's ambient default-privilege/ownership behavior happened to restore it — an assumption that was not verified, in the same spirit as the column-order assumption that already turned out wrong once in this file.

**Fix applied to `supabase/migrations/20260828120000_promote_marketing_contact_to_sales.sql`** (file only — nothing applied to any database):
1. Added two explicit grant statements after the existing anon/authenticated lines:
   `grant select, insert, update, delete, truncate, references, trigger on public.v_sales_lead_inbox to service_role;`
   `grant select, insert, update, delete, truncate, references, trigger on public.v_sales_lead_inbox to postgres;`
   Privileges are spelled out explicitly (matching this file's existing style for the `authenticated` revoke line) rather than using `grant all`, so the statement's effect doesn't depend on exactly which relation-level privileges a given Postgres version's `ALL` expands to.
2. Added a "Grants (confirmed live on production via direct query, not inferred)" comment block recording the four-role table above, and explained why all four are made explicit rather than left to ambient behavior: (a) Supabase's schema-level default privileges are known to auto-grant `anon`/`authenticated` on new `public` schema objects — the reason the original migration already revoked/reduced those two — and that hazard applies identically to a freshly `create view`d object; (b) `service_role`/`postgres` ending up with full access again was not independently verified as guaranteed (via default privileges or via migration-runner ownership) rather than assumed, so both are granted explicitly instead of relying on that assumption.

No other SQL changed: the view's column list/order, the RPC body, and `20260828110000_reconcile_sales_is_test_classification.sql` are unchanged from the previous two entries.

No migration was applied, no push, no merge, no deploy. Validation not re-run in this pass (only `.sql` migration files and this status file were touched).
