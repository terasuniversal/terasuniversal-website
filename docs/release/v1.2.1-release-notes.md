# TERAS Admin CMS v1.2.1

## Security patch

- Upgraded Next.js to `15.5.22`.
- Pinned patched `PostCSS` and `sharp` dependency versions.
- Confirmed `npm audit --omit=dev` reports zero vulnerabilities.

## Compatibility

This release is a patch on v1.2.0 and does not add business modules or change the public website design.
