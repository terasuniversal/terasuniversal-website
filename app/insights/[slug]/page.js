import { notFound } from "next/navigation";
import { insights } from "../../../data/insights";

export function generateStaticParams() { return insights.map(({ slug }) => ({ slug })); }
export async function generateMetadata({ params }) { const { slug } = await params; const item = insights.find((entry) => entry.slug === slug); return item ? { title: `${item.title} | TERAS UNIVERSAL`, description: item.excerpt, alternates: { canonical: `/insights/${item.slug}` }, openGraph: { title: item.title, description: item.excerpt, type: "article" } } : {}; }

export default async function InsightPage({ params }) { const { slug } = await params; const item = insights.find((entry) => entry.slug === slug); if (!item) notFound(); const articleSchema = { "@context": "https://schema.org", "@type": "Article", headline: item.title, description: item.excerpt, articleSection: item.category, author: { "@type": "Organization", name: "TERAS UNIVERSAL SDN. BHD." }, publisher: { "@type": "Organization", name: "TERAS UNIVERSAL SDN. BHD." }, mainEntityOfPage: `https://terasuniversal.com.my/insights/${item.slug}` }; return <main className="insight-article-page"><article className="container insight-article"><nav className="course-breadcrumb" aria-label="Breadcrumb"><a href="/">Home</a><span>/</span><a href="/insights">Insights</a><span>/</span><span aria-current="page">{item.title}</span></nav><span className="eyebrow">{item.category} · {item.readTime}</span><h1>{item.title}</h1><p className="insight-article-lead">{item.excerpt}</p><div className="insight-article-body"><p>Industrial work environments require clear expectations, practical understanding and consistent follow-through. This insight provides a general starting point for teams reviewing their training, supervision and workplace controls.</p><h2>Start with the work and the risk</h2><p>Effective development begins by understanding the role, the operating environment and the situations that can affect people, equipment or continuity. Use that understanding to define the behaviours and capabilities the programme should strengthen.</p><h2>Connect learning with application</h2><p>Learning becomes more valuable when participants can connect principles with realistic scenarios, guided practice and clear feedback. Supervisors and managers should reinforce the same expectations after the session.</p><h2>Review and improve</h2><p>Use observations, participant feedback and operational learning to identify what should be improved next. For organisation-specific advice, speak with the TERAS UNIVERSAL team about a tailored training or consultancy discussion.</p></div><div className="insight-article-cta"><h2>Discuss your workforce requirements</h2><a className="btn btn-gold" href="/request-proposal">Request Proposal <span aria-hidden="true">&rarr;</span></a></div><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(articleSchema) }} /></article></main>; }
