import { notFound } from "next/navigation";
import { requireModuleAccess, requireRole } from "../../../../lib/auth/session";
import { createSupabaseServerClient } from "../../../../lib/supabase/server";
import { PRINT_WHEN_READY_SCRIPT } from "../../../../lib/print-when-ready";
import { formatMalaysiaDate } from "../../../../lib/date-time";
import { revisionLabel, type SalesQuotationItemRow, type SalesQuotationRow } from "../../../../lib/sales/crm";
import { quotationTrainingDetailsSchema } from "../../../../lib/validation/schemas";
import { DocumentFooter, DocumentHeader } from "../../../../components/admin/documents/DocumentHeader";
import { quotationTermsText } from "../../../../lib/documents/company";

export const metadata = { title: "Quotation - TERAS UNIVERSAL", robots: { index: false, follow: false } };
export const dynamic = "force-dynamic";

const NAVY = "#0B3A63";
const GOLD = "#D4AF37";
const MUTED = "#667085";
const BORDER = "#D9E1EA";
const SANS = "Montserrat, Poppins, Inter, Arial, Helvetica, sans-serif";
type PackageEntry = { key?: unknown; label?: unknown };

function fmt(value: number, currency = "MYR") {
  const prefix = currency === "MYR" ? "RM " : `${currency} `;
  return `${prefix}${Number(value || 0).toLocaleString("en-MY", { minimumFractionDigits: 2, maximumFractionDigits: 2 })}`;
}
function packageEntries(value: unknown): PackageEntry[] {
  if (!Array.isArray(value)) return [];
  return value.filter((entry): entry is PackageEntry => Boolean(entry && typeof entry === "object" && typeof (entry as PackageEntry).label === "string"));
}
function valueOrDash(value: string | null | undefined) { return value?.trim() || "-"; }

export default async function QuotationPdfPage({ params }: { params: Promise<{ id: string }> }) {
  await requireRole("editor");
  await requireModuleAccess("sales_quotations");
  const { id } = await params;
  const supabase = await createSupabaseServerClient();
  const { data: quotation } = await supabase.from("sales_quotations").select("*").eq("id", id).maybeSingle();
  if (!quotation) notFound();
  const q = quotation as SalesQuotationRow;
  const trainingResult = quotationTrainingDetailsSchema.safeParse(q.training_details);
  const training = trainingResult.success ? trainingResult.data : null;
  const { data: opportunity } = await supabase.from("sales_opportunities").select("opportunity_no, company_name, contact_person, contact_email, contact_phone, company_id").eq("id", q.opportunity_id).maybeSingle();
  const { data: company } = opportunity?.company_id
    ? await supabase.from("companies").select("company_name, registration_no, email, phone, person_in_charge, pic_email, pic_phone, billing_address, address").eq("id", opportunity.company_id).maybeSingle()
    : { data: null };
  const { data: itemRows } = await supabase.from("sales_quotation_items").select("*").eq("quotation_id", id).order("sort_order");
  const items = (itemRows ?? []) as SalesQuotationItemRow[];
  const customer = {
    company: q.customer_company_name ?? company?.company_name ?? opportunity?.company_name,
    contact: q.customer_contact_name ?? company?.person_in_charge ?? opportunity?.contact_person,
    registration: q.customer_registration_no ?? company?.registration_no,
    email: q.customer_email ?? company?.email ?? company?.pic_email ?? opportunity?.contact_email,
    phone: q.customer_phone ?? company?.phone ?? company?.pic_phone ?? opportunity?.contact_phone,
    billing: q.billing_address ?? company?.billing_address ?? company?.address,
    training: q.training_service_address,
  };

  // Explicit page ownership avoids Chromium's unsupported page counters. The first
  // page reserves space for the customer/programme summary; continuation pages
  // carry six rows each, keeping totals and terms together on the final page.
  const firstPageItems = items.slice(0, 2);
  const continuationItems = items.slice(2);
  const itemPages: SalesQuotationItemRow[][] = [firstPageItems];
  for (let index = 0; index < continuationItems.length; index += 7) itemPages.push(continuationItems.slice(index, index + 7));
  const pageCount = itemPages.length;
  const identity = [{ label: "Quotation No.", value: q.quotation_no }, { label: "Revision", value: revisionLabel(q.revision_no) }, { label: "Issue Date", value: formatMalaysiaDate(q.issue_date) }, { label: "Valid Until", value: q.valid_until ? formatMalaysiaDate(q.valid_until) : "No expiry" }];

  function renderItems(pageItems: SalesQuotationItemRow[]) {
    return <table className="quotation-print-items"><thead><tr><th>No.</th><th>Course / Description / Package Includes</th><th>Qty</th><th>Unit</th><th>Unit Price</th><th>Discount</th><th>Amount</th></tr></thead><tbody>{pageItems.map((item) => { const originalIndex = items.findIndex((row) => row.id === item.id); const packages = packageEntries(item.package_includes_snapshot); return <tr key={item.id}><td>{originalIndex + 1}</td><td>{item.course_name_snapshot && <div className="quotation-print-course">{item.course_name_snapshot}{item.hrdf_claim ? <span className="quotation-print-hrdf"> HRDF</span> : null}</div>}<div className="quotation-print-description">{item.description}</div>{packages.length > 0 && <div className="quotation-print-package"><strong>Package Includes: </strong>{packages.map((entry, packageIndex) => <span key={`${String(entry.key)}-${packageIndex}`}>{String(entry.label)}</span>)}</div>}</td><td>{item.quantity}</td><td>{item.unit}</td><td>{fmt(item.unit_price, q.currency)}</td><td>{fmt(item.discount, q.currency)}</td><td>{fmt(item.line_total, q.currency)}</td></tr>; })}</tbody></table>;
  }

  return <div className="quotation-print-root"><style>{`
    .quotation-print-root{min-height:100vh;padding:24px;background:#EEF2F6;color:#1D2939;font-family:${SANS};font-size:12px}
    .quotation-print-paper{width:794px;min-height:1123px;box-sizing:border-box;margin:0 auto;padding:44px 52px 64px;background:#fff;box-shadow:0 8px 28px rgba(11,58,99,.12);position:relative;break-after:page;page-break-after:always}
    .quotation-print-paper:last-of-type{break-after:auto;page-break-after:auto}
    .teras-document-header{display:flex;justify-content:space-between;gap:28px;padding-bottom:16px;border-bottom:3px solid ${NAVY}}
    .teras-document-brand{min-width:0}.teras-document-logo{width:188px;height:auto;object-fit:contain;object-position:left center}.teras-document-company{margin-top:7px;color:${NAVY};font-size:10px;font-weight:700;letter-spacing:1px}.teras-document-registration{margin-top:4px;color:${MUTED};font-size:10px}.teras-document-address{max-width:290px;margin-top:5px;color:${MUTED};font-size:10px;line-height:1.45}.teras-document-identity{text-align:right}.teras-document-title{color:${NAVY};font-size:25px;font-weight:800;letter-spacing:2px}.teras-document-meta{display:grid;gap:5px 22px;margin-top:10px;color:${MUTED};font-size:11px;text-align:right}.teras-document-meta-row{display:grid;grid-template-columns:1fr auto;gap:22px}.teras-document-meta-row strong{color:#1D2939;font-weight:700}
    .quotation-print-section{margin-top:21px;padding-top:10px;border-top:1px solid #E8EDF3}.quotation-print-section-title{margin:0 0 8px;color:${NAVY};font-size:10px;font-weight:800;letter-spacing:1.1px;text-transform:uppercase}.quotation-print-section-title::after{content:"";display:block;width:24px;margin-top:4px;border-bottom:2px solid ${GOLD}}
    .quotation-print-card-grid{display:grid;grid-template-columns:1fr 1fr;gap:8px}.quotation-print-card{min-height:64px;padding:10px 12px;box-sizing:border-box;border:1px solid #E3E9F0;border-radius:2px}.quotation-print-card-label{margin-bottom:5px;color:${MUTED};font-size:10px;font-weight:700;text-transform:uppercase}.quotation-print-card-value{white-space:pre-wrap;overflow-wrap:anywhere;line-height:1.45}.quotation-print-card-value strong{color:${NAVY}}
    .quotation-print-items{width:100%;border-collapse:collapse;table-layout:fixed}.quotation-print-items th{padding:8px 7px;background:${NAVY};color:#fff;font-size:10px;letter-spacing:.3px;text-align:left}.quotation-print-items td{padding:8px 7px;border-bottom:1px solid ${BORDER};vertical-align:top;line-height:1.45;font-size:11px}.quotation-print-items tr{break-inside:avoid;page-break-inside:avoid}.quotation-print-items thead{display:table-header-group}
    .quotation-print-items th:nth-child(1),.quotation-print-items td:nth-child(1){width:4%;text-align:center}.quotation-print-items th:nth-child(2),.quotation-print-items td:nth-child(2){width:42%}.quotation-print-items th:nth-child(3),.quotation-print-items td:nth-child(3){width:8%;text-align:center}.quotation-print-items th:nth-child(4),.quotation-print-items td:nth-child(4){width:12%;text-align:center}.quotation-print-items th:nth-child(5),.quotation-print-items td:nth-child(5),.quotation-print-items th:nth-child(6),.quotation-print-items td:nth-child(6),.quotation-print-items th:nth-child(7),.quotation-print-items td:nth-child(7){width:14%;text-align:right}
    .quotation-print-course{margin-bottom:5px;color:${NAVY};font-size:12px;font-weight:800}.quotation-print-hrdf{color:${GOLD};font-size:9px;font-weight:800;letter-spacing:.5px}.quotation-print-description{white-space:pre-wrap;overflow-wrap:anywhere}.quotation-print-package{margin-top:7px;color:${MUTED};font-size:10px}.quotation-print-package span:not(:last-child)::after{content:"  |  ";color:${GOLD}}
    .quotation-print-totals-wrap{display:flex;justify-content:flex-end;margin-top:14px;break-inside:avoid;page-break-inside:avoid}.quotation-print-totals{width:270px;border-collapse:collapse;font-size:11px}.quotation-print-totals td{padding:5px 8px}.quotation-print-totals td:last-child{text-align:right}.quotation-print-grand td{padding-top:8px;border-top:2px solid ${NAVY};border-bottom:2px solid ${GOLD};background:#F5F8FB;color:${NAVY};font-size:14px;font-weight:800}
    .quotation-print-terms{white-space:pre-wrap;overflow-wrap:anywhere;line-height:1.5}.teras-document-running-footer{position:absolute;bottom:22px;left:52px;right:52px;padding-top:8px;border-top:1px solid ${BORDER};background:#fff;color:${MUTED};font-size:8px;display:flex;justify-content:space-between;gap:12px;z-index:2}
    @media print{html,body{margin:0!important;padding:0!important}.quotation-print-root{width:auto;min-height:0;padding:0;background:#fff;font-size:10px}.quotation-print-paper{width:210mm;height:297mm;min-height:297mm;margin:0;padding:10mm 12mm 20mm;box-shadow:none;break-after:page;page-break-after:always}.quotation-print-paper:last-of-type{break-after:auto;page-break-after:auto}.teras-document-logo{width:118px}.teras-document-company{font-size:10px}.quotation-print-section{margin-top:11px;padding-top:6px}.quotation-print-card{min-height:0;padding:7px 9px}.quotation-print-section-title{margin-bottom:6px}.quotation-print-items th{padding:5px}.quotation-print-items td{padding:6px 5px;font-size:10px}.quotation-print-package{margin-top:5px}.quotation-print-totals-wrap{margin-top:8px}.quotation-print-totals td{padding:3px 6px}.quotation-print-grand td{padding-top:6px}.teras-document-header,.quotation-print-card,.quotation-print-totals-wrap{break-inside:avoid;page-break-inside:avoid}*{-webkit-print-color-adjust:exact!important;print-color-adjust:exact!important}}
    @media screen and (max-width:840px){.quotation-print-root{padding:10px;overflow-x:auto}.quotation-print-paper{margin:0}}
  `}</style>{itemPages.map((pageItems, pageIndex) => <main className="quotation-print-paper" key={pageIndex}><DocumentHeader type="QUOTATION" identity={identity} />{pageIndex === 0 && <><section className="quotation-print-section"><h2 className="quotation-print-section-title">Customer</h2><div className="quotation-print-card-grid"><div className="quotation-print-card"><div className="quotation-print-card-label">Customer / Company</div><div className="quotation-print-card-value"><strong>{valueOrDash(customer.company)}</strong></div></div><div className="quotation-print-card"><div className="quotation-print-card-label">Attention</div><div className="quotation-print-card-value">{valueOrDash(customer.contact)}</div></div><div className="quotation-print-card"><div className="quotation-print-card-label">Registration No.</div><div className="quotation-print-card-value">{valueOrDash(customer.registration)}</div></div><div className="quotation-print-card"><div className="quotation-print-card-label">Contact</div><div className="quotation-print-card-value">{valueOrDash(customer.email)}{customer.phone ? `\n${customer.phone}` : ""}</div></div></div></section><section className="quotation-print-section"><h2 className="quotation-print-section-title">Addresses</h2><div className="quotation-print-card-grid"><div className="quotation-print-card"><div className="quotation-print-card-label">Billing Address</div><div className="quotation-print-card-value">{valueOrDash(customer.billing)}</div></div>{customer.training && <div className="quotation-print-card"><div className="quotation-print-card-label">Training / Service Address</div><div className="quotation-print-card-value">{customer.training}</div></div>}</div></section>{training && <><section className="quotation-print-section"><h2 className="quotation-print-section-title">Programme Details</h2><div className="quotation-print-card-grid"><div className="quotation-print-card"><div className="quotation-print-card-label">Programme</div><div className="quotation-print-card-value"><strong>{valueOrDash(training.programme.course_name_snapshot)}</strong></div></div><div className="quotation-print-card"><div className="quotation-print-card-label">Date / Duration</div><div className="quotation-print-card-value">{training.programme.start_date || "—"} → {training.programme.end_date || "—"}{training.programme.duration_label ? `\n${training.programme.duration_label}` : ""}</div></div><div className="quotation-print-card"><div className="quotation-print-card-label">Participants</div><div className="quotation-print-card-value">{training.participants.count} Pax{training.participants.tbc || training.participants.names.length === 0 ? "\nTo Be Confirmed (TBC)" : `\n${training.participants.names.join("\n")}`}</div></div><div className="quotation-print-card"><div className="quotation-print-card-label">Venue / Location</div><div className="quotation-print-card-value"><strong>{training.venue.type === "teras_hq" ? "TERAS HQ" : "In-House"}</strong>{training.venue.name ? `\n${training.venue.name}` : ""}{training.venue.address ? `\n${training.venue.address}` : ""}</div></div></div></section><section className="quotation-print-section"><h2 className="quotation-print-section-title">Training Package Summary</h2><div className="quotation-print-card-grid"><div className="quotation-print-card"><div className="quotation-print-card-label">Accommodation</div><div className="quotation-print-card-value">{training.accommodation.included ? `Included${training.accommodation.nights ? ` — ${training.accommodation.nights} nights` : ""}` : "Not Included"}{training.accommodation.description ? `\n${training.accommodation.description}` : ""}</div></div><div className="quotation-print-card"><div className="quotation-print-card-label">Meals</div><div className="quotation-print-card-value">{training.meals.included ? `Included${training.meals.meals_per_day ? ` — ${training.meals.meals_per_day} per day` : ""}` : "Not Included"}{training.meals.description ? `\n${training.meals.description}` : ""}</div></div><div className="quotation-print-card" style={{ gridColumn: "1 / -1" }}><div className="quotation-print-card-label">Additional Inclusions</div><div className="quotation-print-card-value">{[training.inclusions.training_notes ? "Training notes" : null, training.inclusions.practical_assessment ? "Practical assessment" : null, training.inclusions.certificate ? "Certificate" : null, ...training.inclusions.other].filter(Boolean).join("  |  ") || "-"}</div></div></div></section></>}</> }<section className="quotation-print-section quotation-print-items-section"><h2 className="quotation-print-section-title">{pageIndex === 0 ? "Commercial Breakdown" : "Commercial Breakdown — Continued"}</h2>{renderItems(pageItems)}</section>{pageIndex === pageCount - 1 && <><div className="quotation-print-totals-wrap"><table className="quotation-print-totals"><tbody><tr><td>Subtotal</td><td>{fmt(q.subtotal, q.currency)}</td></tr><tr><td>Discount</td><td>{fmt(q.discount, q.currency)}</td></tr><tr><td>{q.sst_applicable ? `SST (${q.sst_rate}%)` : "Tax (not applicable)"}</td><td>{fmt(q.tax, q.currency)}</td></tr><tr className="quotation-print-grand"><td>Grand Total</td><td>{fmt(q.total, q.currency)}</td></tr></tbody></table></div>{(q.terms || q.notes || q.valid_until) && <section className="quotation-print-section"><h2 className="quotation-print-section-title">Terms / Notes</h2><div className="quotation-print-terms"><strong>Terms</strong><br />{quotationTermsText(q.terms, q.valid_until, formatMalaysiaDate)}</div>{q.notes && <div className="quotation-print-terms" style={{ marginTop: 10 }}><strong>Notes</strong><br />{q.notes}</div>}</section>}{training?.inclusions.training_notes && <section className="quotation-print-section"><h2 className="quotation-print-section-title">Training Notes</h2><div className="quotation-print-terms">Training notes are included as part of the quotation snapshot.</div></section>}</>}<DocumentFooter documentNumber={q.quotation_no} customerName={customer.company} pageNumber={pageIndex + 1} pageCount={pageCount} /></main>)}<script dangerouslySetInnerHTML={{ __html: PRINT_WHEN_READY_SCRIPT }} /></div>;
}
