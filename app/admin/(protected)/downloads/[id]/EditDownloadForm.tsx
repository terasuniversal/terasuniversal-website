"use client";
import { useActionState } from "react";
import Link from "next/link";
import { Field } from "../../../../../components/admin/ui";
import { updateDownload } from "../actions";
export function EditDownloadForm({ item }: { item: any }) { const [state, action, pending] = useActionState(updateDownload.bind(null, item.id), { message: "" }); return <form action={action} className="ta-form" style={{ maxWidth: 820 }}>{state.message && <div className="ta-alert ta-alert-error">{state.message}</div>}<Field label="Title" name="title"><input id="title" name="title" required defaultValue={item.title} /></Field><Field label="File URL" name="file_url"><input id="file_url" name="file_url" type="url" required defaultValue={item.file_url} /></Field><Field label="Description" name="description"><textarea id="description" name="description" rows={4} defaultValue={item.description ?? ""} /></Field><Field label="Category" name="category"><input id="category" name="category" defaultValue={item.category ?? ""} /></Field><Field label="Status" name="status"><select id="status" name="status" defaultValue={item.status}><option value="draft">Draft</option><option value="published">Published</option><option value="archived">Archived</option></select></Field><div className="ta-form-actions"><Link href="/admin/downloads" className="ta-btn ta-btn-outline">Cancel</Link><button className="ta-btn ta-btn-primary" disabled={pending}>Save changes</button></div></form>; }
